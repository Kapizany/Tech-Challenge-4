services:
  stock-forecast-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stock-forecast-api
    environment:
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_SESSION_TOKEN: ${AWS_SESSION_TOKEN:-}
      AWS_DEFAULT_REGION: ${AWS_DEFAULT_REGION:-us-east-1}
      DATA_S3_BUCKET: ${DATA_S3_BUCKET:-}
      DATA_S3_PREFIX: ${DATA_S3_PREFIX:-}
      MODELS_S3_BUCKET: ${MODELS_S3_BUCKET:-}
      MODELS_S3_PREFIX: ${MODELS_S3_PREFIX:-}
      TRACE_REQUESTS: ${TRACE_REQUESTS:-true}
      TRACE_LOG_BODIES: ${TRACE_LOG_BODIES:-true}
      TRACE_MAX_BODY_CHARS: ${TRACE_MAX_BODY_CHARS:-4000}
      TRACE_EXCLUDED_PATHS: ${TRACE_EXCLUDED_PATHS:-/health,/metrics}
      PYTHONPATH: /app/src
      PORT: "8000"
    ports:
      - "8000:8000"
    restart: unless-stopped
